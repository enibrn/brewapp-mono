export default defineNuxtConfig({
  extends: '../base-red/nuxt.config.js',
  build: {
    // Eventuali configurazioni di build specifiche per app-red
  },
  plugins: [
    // Eventuali plugin specifici per app-red
  ],
  // Altre configurazioni specifiche per app-red
})