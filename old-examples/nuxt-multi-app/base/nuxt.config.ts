import { defineNuxtConfig } from 'nuxt'

export default defineNuxtConfig({
  // Configurazione specifica per l'app "base"
  build: {
    // Opzioni di build
  },
  modules: [
    // Moduli utilizzati
  ],
})